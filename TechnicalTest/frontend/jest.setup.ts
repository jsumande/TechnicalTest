/**
 * Jest setup file — runs after the test framework is installed.
 * Imports @testing-library/jest-dom to add custom matchers like:
 *   toBeInTheDocument(), toHaveTextContent(), toBeVisible(), etc.
 */
import '@testing-library/jest-dom';
